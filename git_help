~~~~~~~~~
Fetch remote copy of current branch and immediately merge it into local copy.

$ git pull <remote> [--no-commit]
~~~~~~~~~
Push local files to specified branch in remote repository

$ git push <remote> <branch>
~~~~~~~~~
update changes from local to remote

$ git add --all
$ git commit "commit info"
$ git push [remote (origin)] [branch]